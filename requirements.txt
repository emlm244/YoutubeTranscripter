youtube-transcript-api>=1.0.0
yt-dlp
faster-whisper
openai
websocket-client

# CUDA 12 runtime for ctranslate2 (faster-whisper backend)
# Required for faster-whisper's CTranslate2 backend
nvidia-cublas-cu12
nvidia-cuda-runtime-cu12

numpy>=1.24.0
sounddevice>=0.4.6
noisereduce>=3.0.0

# PyTorch nightly with CUDA 12.8 support for RTX 5080 (Blackwell/sm_120)
# Note: Using nightly builds until stable PyTorch supports Blackwell GPUs
# Install manually: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
torch
torchvision
torchaudio

PyQt6>=6.5.0

# Grammar correction
# Primary: GECToR (GPU-accelerated grammar correction)
# Fallback: LanguageTool (local, no API key needed)
language_tool_python>=2.7.0

# GECToR dependencies
gector @ git+https://github.com/gotutiyan/gector.git
transformers>=4.53.0,<5.0.0
